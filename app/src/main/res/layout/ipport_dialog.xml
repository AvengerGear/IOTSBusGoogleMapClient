<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context=".IPPortDialog" >

    <EditText
        android:id="@+id/serverip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        android:layout_marginBottom="16dp"
        android:fontFamily="sans-serif"
        android:hint="Server IP"
        android:text="192.168.2.1"/>

    <EditText
        android:id="@+id/virtualserverip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/serverip"
        android:layout_below="@+id/serverip"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:hint="Virtual Server IP"
        android:text="192.168.2.4"/>

</RelativeLayout>